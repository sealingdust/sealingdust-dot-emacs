(add-to-list 'load-path "~/.emacs.d")
;; load the golobal-settings.el where the 
(load "dust-global-settings.el")

;; Require my plugins.el
(load "dust-plugins-settings.el")

;; Require key bindings
(load "dust-key-bindings.el")

